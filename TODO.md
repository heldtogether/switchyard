# To dos

1. Run status accuracy: investigate how run status is set server‑side; ensure it
    transitions to terminal once all jobs are terminal (and reflected in the UI).
2. Job labels: use job name/slug as primary label; image name as secondary label in all job
    lists and tables.
3. Navigation UX: review active link styling and add clear breadcrumbs for runs/jobs to
    improve orientation.
4. Fix warnings in the UI dir, primarily for importing React for no reason.
5. Update the README / ARCHITECTURE / Other Docs to ensure the UI is reference
